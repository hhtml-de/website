<nav class="topnav" id="top-nav" aria-label="Top navigation">
  <ul>
  {% for item in navigation.items %}
    <li>
      <a class="button" href="{{ meta.url }}{{ item.href }}" {{ helpers.getLinkActiveState(item.href, page.url) | safe }}>{{ item.text }}</a>
    </li>
  {% endfor %}
  </ul>
</nav>
